using UnityEngine;
using UnityEngine.UI;
//using DG.Tweening;

public class MainMenu : MonoBehaviour
{
    public Button playButton;
    
    private void Start() {
       //playButton.gameObject.SetActive(true);
      // playButton.gameObject.transform.DOScale(new Vector3(300f, 300f, 300f),1);
     //playButton.transform.DOPunchScale(new Vector3(3, 3, 3), 2);
    }

}
